# Deployment için GitHub Actions kullanılmıyor.
# Hostinger VPS'te OpenLiteSpeed + PM2 ile manuel deploy yapılıyor.
# Yeni release için: ssh ile VPS'e bağlanın ve `bash scripts/deploy.sh` çalıştırın.
